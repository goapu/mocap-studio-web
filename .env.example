# Optional overrides. Set these in your shell before running the server.
# Defaults are .local/sessions and .local/models inside this checkout.
# MOCAP_DATA_DIR=/absolute/path/to/local/sessions
# MOCAP_MODEL_DIR=/absolute/path/to/local/models
# MOCAP_PYTHON=/absolute/path/to/existing/venv/bin/python
